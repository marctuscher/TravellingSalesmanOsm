{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "npm",
            "script": "build",
            "path": "react-client/",
            "problemMatcher": []
        }, 
        {
            "label": "make debug",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/src/"
            },
            "command": "rm -rf test* && make test"
        }
    ]
}